<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full min-h-screen bg-zinc-800 text-white p-10">
            <div class="flex items-start gap-3">
                <div class="w-10 h-10  rounded-md">
                   <img class="w-full h-full object-cover "src="/images/uploads/<%= user.profile %>" alt="image not found">
                </div>
                <h3 class="text-3xl mb-5"> Hello,What's up <%=user.name %></h3>
            </div>
           
            <form method="post"action="/post">
                <textarea placeholder="write your post here" class=" p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-100 border-yellow-1000" name="textcontent"></textarea>
                <br>
                <button class="px-3 py-2 mb-5 rounded-md bg-yellow-200  border-2 text-black" value="submit">Submit</button>
            </form>
            <a  href="/logout" class="px-3 py-2 rounded-md bg-red-500  border-2 text-black" value="submit">Logout</a>
            <div class="posts mt-20">
                <h3 class="text-zinc-400"> Your Post</h3>
                <div class="postcontainer">

                    <% user.posts.forEach(function(post){ %>

                     <div class="post w-1/3 mb-2 p-5 border-2 border-zinc-500 rounded-md">
                        <h4 class="text-blue-500"><%= user.name %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mb-2"><%= post.likes.length %> likes</small>
                        <div class="flex gap-4 btns mt-2">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                               <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-green-500" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>

                   <% })%>              
                </div>
            </div>   
    </div>
    
</body>
</html>